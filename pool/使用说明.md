# 提交任务

```Go
ctx := context.WithValue(context.Background(), "trace_id", "123456")
err := pool.SubmitTaskWithRetry(ctx, func(ctx context.Context) {
    // 业务逻辑
    logger.Sugar.Info("execute task", zap.String("trace_id", ctx.Value("trace_id").(string)))
}, 3, 100*time.Millisecond)
if err != nil {
    // 处理最终失败逻辑
}
```

# 优雅关闭
```Go
// 等待最多5秒，让剩余任务执行完成
err := pool.GlobalPool.Close(5 * time.Second)
```

# 监控状态
```Go
stats := pool.GlobalPool.GetPoolStats()
logger.Sugar.Info("pool stats", zap.Any("stats", stats))
```